export function Footer() {
  return (
    <footer className="site-footer" id="method">
      <div>
        <p className="eyebrow">Method</p>
        <h2>Designed once. Adapted endlessly.</h2>
      </div>
      <p>
        Every blueprint will be built around modular UI, brand tokens, clear setup
        instructions, and sensible domain defaults so business owners can skip the blank
        page and focus on the work they actually want to do.
      </p>
    </footer>
  );
}
